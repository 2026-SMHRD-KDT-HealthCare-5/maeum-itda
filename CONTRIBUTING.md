# 🌿 Git 브랜치 전략 및 작업 플로우 가이드

팀의 효율적인 협업과 안정적인 배포를 위한 Git 워크플로우 및 커밋 컨벤션 가이드입니다.

---

## 📌 브랜치 전략 (Branch Strategy)

| 브랜치명 | 설명 | 비고 |
| :--- | :--- | :--- |
| **`main`** | 제품 출시 및 라이브 서비스 배포 브랜치 | 항상 출시 가능한 상태 유지 |
| **`dev`** | 다음 출시 버전을 개발하는 통합 브랜치 | 개발의 기본 베이스 (기본 브랜치) |
| **`feat/기능이름`** | 개별 기능을 개발하는 피처 브랜치 | `dev` 브랜치에서 분기 및 병합 |

---

## 🔄 Git / Bash 표준 작업 플로우

### 1️⃣ 새로운 기능 개발을 시작할 때
> 💡 항상 `dev` 브랜치의 최신 코드를 기준으로 새로운 피처 브랜치를 생성합니다.

```bash
# 1. 최신 코드를 가져오기 위해 dev 브랜치로 이동
git checkout dev

# 2. 원격 저장소의 최신 변경 사항 반영
git pull origin dev

# 3. 새로운 기능 개발을 위한 피처 브랜치 생성 및 이동
git checkout -b feat/기능이름

```

### 2️⃣ 기능 개발 중 작업을 커밋할 때

> 💡 작업 단위별로 명확한 커밋 메시지와 함께 수시로 저장합니다.

```bash
# 1. 변경된 모든 파일 스테이징
git add .

# 2. 커밋 컨벤션에 맞춰 메시지 작성
git commit -m "feat: 구현 기능"

```

### 3️⃣ 작업 완료 후 최신 코드 동기화 및 충돌 해결

> 💡 원격 `dev` 브랜치의 최신 상태를 로컬 피처 브랜치로 가져와 병합하고 충돌을 해결합니다.

```bash
# 1. 원격 저장소의 최신 이력 업데이트
git fetch origin

# 2. 원격 dev 브랜치의 변경 사항을 현재 피처 브랜치에 머지
git merge origin/dev

```

> ⚠️ **🚨 중요 Check Point**
> 머지 완료 후, **로컬 환경에서 프로그램이 정상적으로 빌드되는지 및 기능 테스트를 반드시 수행**하여 코드가 깨지지 않았는지 검증하세요.

### 4️⃣ 원격 저장소 Push 및 Pull Request (PR) 생성

> 💡 동기화와 테스트가 완료된 코드를 원격에 올리고 코드 리뷰를 요청합니다.

```bash
# 1. 원격 저장소에 피처 브랜치 push
git push origin feat/기능이름

```

* **이후 후속 작업:** GitHub / GitLab 등 원격 저장소 플랫폼에 접속하여 `feat/기능이름` ➔ `dev` 브랜치 방향으로 **PR을 생성하고 코드 리뷰를 진행**합니다.

---

## 🧹 PR 승인(Merge) 완료 후 브랜치 정리

코드 리뷰가 끝나고 `dev` 브랜치로 병합이 완료되면, 로컬 환경을 깨끗하게 정리합니다.

### 1️⃣ 원격 저장소 최신 상태 반영

```bash
# 1. dev 브랜치로 복귀
git checkout dev

# 2. 병합이 완료된 최신 dev 코드 다운로드
git pull origin dev

```

### 2️⃣ 역할이 끝난 로컬 브랜치 삭제

```bash
# 사용이 끝난 로컬 피처 브랜치 삭제
git branch -d feat/기능이름

```

---

## 📝 커밋 컨벤션 (Commit Convention)

일관된 커밋 메시지는 프로젝트 히스토리 추적을 용이하게 합니다. 아래 접두사를 준수하여 작성해 주세요.

* ✨ **`feat`** : 새로운 기능 추가
```bash
git commit -m "feat: 로그인 API 구현"

```


* 🐛 **`fix`** : 버그 픽스 및 오류 수정
```bash
git commit -m "fix: 회원가입 이메일 중복 체크 오류 수정"

```


* ♻️ **`refactor`** : 코드 개선 (기능 변경이 없는 코드 구조 및 리팩토링)
```bash
git commit -m "refactor: 인증 미들웨어 로직 구조 개선"

```


* 💄 **`style`** : 코드 포맷팅, 세미콜론 누락, CSS 변경 (코드 의미에 영향이 없는 수정)
```bash
git commit -m "style: 메인 페이지 컴포넌트 스타일 수정 및 린트 적용"

```


* 🔧 **`chore`** : 패키지 매니저 설정, 빌드 업무 변경, `.gitignore` 수정 등 (기능 외 설정)
```bash
git commit -m "chore: dotenv 패키지 추가"

```

* 📄 **`docs`** : 문서(`README.md`, `CLAUDE.md`, `docs/` 등)만 수정 (코드 변경 없음)
```bash
git commit -m "docs: sprint-plan.md 최신화"

```