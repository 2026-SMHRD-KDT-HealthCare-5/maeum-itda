// TODO(UC-00-1): local state/hooks for this action. Not implemented yet — the
// UI already manages its own confirm-step state (see ui/index.tsx); this file
// is where a real disconnect API call's pending/error state would live.
export {}
