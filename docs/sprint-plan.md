# 2주 스프린트 계획

- 인원: (프론트엔드·PM·UI/UX) 1명, (백엔드·DB) 1명, (모델링·AI서버) 1명
- 기간: 영업일 기준 10일
- 목표: 폰(PWA)으로 시연 가능한 상태로 마무리 — **실시간 웹 푸시 알림 포함, 스트레치 아닌 필수 범위**
- 화면 범위: `docs/screens`에 있는 12개 화면 전부 구현 (2026-08-16 재확정: 스코프 컷 없이 8/20까지 전부 완료 — 관리자 화면은 애초에 이 12개에 미포함, 별도 제외)
- 모바일 전략: PWA로 확정 (Flutter WebView+FCM 하이브리드는 졸업 이후 단계로 보류)
- 프로토콜 기준 문서: [ws-protocol.md](ws-protocol.md) — 세션/turn 컨테이너 개념 없음, `CONVERSATION_MESSAGE` 행 단위 저장 + `generationId` 기반 무효화가 현재 백엔드 설계

> **2026-08-10 업데이트**: `packages/shared-types`가 아직 옛 모델(`ConversationTurn`, `VoiceCapturedEvent` 등 `type` 판별 유니언 이벤트)이라 `ws-protocol.md`(event/payload/ts, 메시지 행 모델)와 불일치 상태였음을 확인. 프론트 3개 파일(`SeniorDailyRecordPage.tsx`, `entities/conversation/ui`, `entities/conversation/model`)이 옛 계약을 참조 중. **이 계약 재정비를 Day 0/Day 1 최우선 작업으로 승격**.

> **2026-08-12 백엔드 업데이트**: NestJS 단독 구현과 Mock 테스트는 완료됐다. 8월 21일까지의 백엔드 중점 범위는 새 기능을 늘리는 것이 아니라 `Frontend ↔ NestJS WebSocket` 실제 연동, `NestJS ↔ FastAPI REST` 실제 연동, 통합 오류 처리와 E2E 검증이다. 프론트와 AI 서버 담당자의 기존 작업 계획은 유지하고, 아래 BE/DB 일정만 현재 구현 상태를 반영해 수정한다.

> **2026-08-13 전체 재구성 (Day 2, 실제 구현 상태 재조사 기반)**: Day 0에서 짠 원래 일정을 부분 수정하는 대신, 현재 3개 계층(frontend/backend/ai-server)을 전부 다시 조사해 남은 8영업일(Day 2~Day 10) 일정을 재구성했다. 조사 결과 원래 계획과 다르게 앞서간 부분과 뒤처진 부분이 동시에 있었고, **원래 목록에 없던 새 블로커 2개(로그인 API 부재, AI서버-백엔드 프로토콜 불일치)**가 발견됐다. 아래 "재조사 결과"와 "신규 리스크"를 먼저 읽고 Week 1/2 표를 봐야 맥락이 맞는다.

> **2026-08-15 최종 재구성 (인원 가용 일정 변경)**: 팀 가용성이 바뀌었다 — **8/15(토)~8/17(월)은 AI서버/백엔드 담당 1인만 작업, 8/18(화)~8/20(목)은 전원 작업, 8/21 이후는 팀 전원 포함 일정 확신 없음.** 이에 따라 이 문서의 계획 범위를 8/13~8/25(Week1/Week2)에서 **8/15~8/20으로 재설정**하고, 아래 "최종 크런치 일정"으로 기존 Week 1/2 표를 대체한다. 8/14 재조사에서 신규 리스크 1)로그인 API 부재, 2)AI서버-백엔드 프로토콜 불일치는 이미 해소된 것으로 확인됐다. 8/15 재조사에서는: 감정분석 모델 체크포인트 문제는 팀이 `EMOTION_MODE=test`로 이번 스프린트 기간 우회하기로 `apps/ai-server/models/MODEL_POLICY.md`에 정책화, TTS 응답 필드 드롭 버그는 AI서버 쪽은 완전히 해결됨, 웹 푸시 백엔드(`notifications` 모듈)와 시니어/보호자 연결화면 2개는 이미 대부분 완료된 것으로 확인됨(→ 크런치 일정에 반영). 반면 **척도 채점(SGDS-K/GAD-7/LSNS-6) 실구현은 8/13 시점과 변화 없이 여전히 스텁** — 유일하게 그대로 남은 최우선 리스크.

> **2026-08-16 스코프 최종 확정**: 8/15 재구성 때 나열했던 "축소/보류" 후보들을 팀 결정으로 확정했다.
> - **회원가입 화면**: 이미 구현+API 연동 완료 확인됨 — 크런치 작업 목록에서 완전히 제외(할 일 없음).
> - **감정분석 실모델 연결**: 컷 아님. 당장은 `EMOTION_MODE=test`로 두되, **8/20 이전에 반드시 실모델(`EMOTION_MODE=model`)로 전환 완료**해야 함 — 아래 8/18 일정에 반영.
> - **STT 실시간 반영 / TTS 오디오 전달**: 구현 방식(배치 vs 스트리밍 등)은 자유, 다만 **8/17 이전에 기능 자체는 무조건 완성**해야 함 — 원래 8/17 솔로 계획에 이미 포함돼 있음, 방식 제약만 해제.
> - **Redis 기반 완전 재연결 복구**: 이번 스프린트 범위에서 제외 확정.
> - **위험 키워드 실시간 감지(자해/자살 신호 등 실시간 위험 알림 트리거)**: 삭제 확정 — 결정사항 로그의 기존 MVP 제외 결정을 재확인. AI서버 시스템 프롬프트에 남아있는 안전 확인 톤 전환 문구는 이 기능과 무관하니 유지해도 됨(실제 알림 발송 트리거만 없으면 됨).
> - **관리자 화면**: 삭제 확정 — 애초에 12개 화면에 포함되지 않음, 착수하지 않는다.

> **2026-08-15 저녁 업데이트 (실제 진행 상황 + 신규 리스크)**: 척도 채점이 계획보다 앞서갔다 — `apps/ai-server/app/services/llm_prompts.py`에 SGDS-K(15)/GAD-7(7)/LSNS-6(6) 문항을 고정 문항 은행으로 박아 넣고 SYSTEM_PROMPT에 채점 규칙을 포함시켜서, GAD-7 하나만이 아니라 **3개 척도 전부 코드 상 완료**됐다(`SCALE_ANALYSIS_MODE=model`로 전환하면 사용, 기본값은 여전히 `test`). AI 페르소나도 '다슬'(손녀 컨셉, 존댓말 유지)로 SYSTEM_PROMPT에 명시했다. 단, **실제 `OPENAI_API_KEY`로 채점 품질을 검증한 적은 아직 없다**(unit test는 OpenAI 응답을 mock으로 흉내낸 것만 통과) — 8/16 작업에 이월.
>
> 이 작업 중 스케줄에 없던 리스크 2개를 새로 발견했다:
> 1. **척도 문항 커버리지 컨텍스트가 시스템 어디에도 전달되지 않는다** — NestJS `QuestionAnswerBatch` 계약(`audio-analysis.contract.ts`)에도, FastAPI `/analysis/audio/batch` 엔드포인트(`main.py`)에도 "오늘 이미 채점된 문항"을 넘기는 필드가 없다. AI서버의 `SessionState`는 매 REST 요청마다 새로 생성되며(`session_manager.py`의 싱글턴 미사용) `pending_scale_items`/`prev_session_summary`는 항상 빈 값이다. 즉 FR-01-04("오늘 아직 채점 안 된 문항 유도")를 실제로 지킬 방법이 아직 없다 — 8/16 작업에 반영.
> 2. **`docs/ws-protocol.md` §8이 stale** — "MVP 이후"란에 아직 "TTS binary 전송"이 남아있는데, 8/16 스코프 확정에서 이미 TTS를 8/17 전 필수 완성으로 정했다(배치 base64 방식, 이 문서에 반영 안 됨). TTS 배관 작업 중 이 문서도 같이 갱신할 것.
>
> **TTS는 여전히 100% 미착수** — AI서버(`schemas.py`/`main.py`)는 `ttsAudioBase64`/`ttsMimeType`을 이미 응답에 담고 있지만, 백엔드 `audio-analysis.contract.ts`의 `QuestionAnswerAnalysisResult`엔 해당 필드가 없어 검증기가 버리고, `tts:audio` 같은 WS 이벤트도 코드 어디에도 없으며, 프론트에는 오디오 재생 컴포넌트 자체가 없다. 8/16의 최우선 항목.

## 재조사 결과 (2026-08-13 기준 실제 구현 상태)

**프론트엔드**

- 디자인 적용: Codex 트랙에서 별도 진행 중, 보호자 화면 몇 개만 남고 거의 끝난 상태. 이 작업은 이 일정표의 관리 대상이 아님(Claude→Codex 인수인계 트랙).
- `entities/conversation`, `SeniorDailyRecordPage.tsx`의 옛 계약(`ConversationTurn`/`VoiceCapturedEvent`) 마이그레이션: **완료**. 코드베이스 전체에 옛 타입 참조 없음.
- 시니어 대화 화면(`ChatSocket`, `record-voice-answer`): 실제 `/ws/chats` 연동 코드 완료(Mock 아님) — 원래 Day 3~5 목표를 이미 달성. 단, 로그인이 막혀 있어 브라우저 E2E 확인 불가(아래 참고).
- 보호자 일간 리포트 화면: 디자인 반영 + 데이터 연동 상당 부분 진행됨(최근 커밋 반영).
- PWA 셸(manifest + service worker): **미착수**. 웹 푸시 구독 등록 플로우도 미착수.
- 로그인: `mockResolveRole()`로 역할만 흉내내는 상태 유지 — 아래 백엔드 사유로 계속 블록.

**백엔드(NestJS)**

- `auth`: `check-login-id`, `signup`만 구현되어 있고 **실제 로그인(sign-in) 엔드포인트가 없음**. JWT 발급 로직(`verifyAccessToken`)은 있지만 발급 자체를 트리거하는 라우트가 없어 프론트가 토큰을 받을 방법이 아직 없음.
- `chats`(WS): `ws-protocol.md`의 모든 이벤트와 `GET /chats/messages`까지 NestJS 단독 기준 완료. `packages/shared-types`와도 드리프트 없음.
- `analysis`: 오디오 분석 상태 조회·재시도 엔드포인트와 `AiClient`(FastAPI 호출부) 구현 완료.
- `users`: 서비스 로직(TypeORM 연동)은 있으나 컨트롤러에 라우트가 하나도 없음(내부 호출용으로만 쓰임).
- `reports`, `notifications`: 엔티티만 정의되고 컨트롤러/서비스는 빈 껍데기.
- 웹 푸시(PushSubscription 저장, VAPID, 실제 발송): **코드 전혀 없음**.

**AI 서버(FastAPI)** — "초기 세팅만"이라는 전제가 틀렸음, 실제로는 상당히 진행됐지만 **엉뚱한 프로토콜로 진행됨**:

- STT(OpenAI 우선 + faster-whisper 폴백), 꼬리질문 생성(OpenAI), TTS(Typecast 스트리밍): 실제 구현 완료.
- 감성분석: 텍스트/음성 추론 함수는 있으나 모델 로딩부가 TODO 스텁, 융합(fusion)도 임시 가중평균 가정 로직.
- SGDS-K/GAD-7/LSNS-6 척도채점: **코드 없음.** LLM 프롬프트에 문항 유도용으로만 언급, 실제 채점 로직 자체가 존재하지 않음.
- **치명적 불일치**: 백엔드 `AiClient`는 `POST /analysis/audio/batch`(REST, multipart 배치)를 호출하도록 짜여 있는데, AI 서버는 이 엔드포인트를 아예 구현하지 않고 `/ws/counsel/{session_id}`라는 자체 WebSocket 세션 프로토콜(`session_init`/`utterance_start`/`turn_result`/`tts_chunk_meta`/`tts_end`)을 구현함. Day 0에서 합의했어야 할 BE↔AI 계약이 실제로는 서로 다른 두 프로토콜로 갈라진 채 진행된 것.

## 신규 리스크 (원래 계획에 없던 블로커, 우선 해결 필요)

1. **로그인 API 부재** — 프론트의 모든 실연동(WS `auth` 이벤트 포함)이 여기서 막힘. `POST /auth/login` 구현이 다른 무엇보다 먼저 필요.
2. **AI서버↔백엔드 프로토콜 불일치** — REST 배치냐 WS 세션이냐 팀이 다시 정해야 함. 그대로 두면 8/18(재조정된 통합 체크포인트)에 연결 자체가 안 됨.
3. **척도채점(SGDS-K/GAD-7/LSNS-6) 0% 구현** — 제품 핵심 가치(TextScore 산출)인데 코드가 전혀 없음. 원래 Week2 중반 일정이었지만 AI서버 인력이 프로토콜 재작업에 묶이면 더 밀릴 위험.
4. **웹 푸시 인프라 전무** — 백엔드 구독 저장 API가 없으니 프론트 SW 구독 플로우도 나란히 멈춰 있음. 필수 범위 확정 항목이라 스코프 컷 대상이 아니므로 착수 순서를 앞당겨야 함.

## 화면 우선순위 (docs/screens 기준, 12개 전부 구현)

| 티어                   | 화면                                                                                                                  | 비고                                                                                                                                          |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 (골든패스, 최우선)   | 로그인, 시니어 홈, **시니어 대화 화면**, 보호자 대시보드, 보호자 일간 리포트 조회                                     | 시니어 대화 화면 자체는 실연동 완료. 로그인만 API 부재로 막힘 — 이 티어의 실제 잔여 작업은 "로그인 API"에 집중됨                              |
| 2 (데모에 있으면 좋음) | 회원가입, 시니어-보호자 연결 화면(2개: senior-connection/guardian-connection), 보호자 알림함, 보호자 주간 리포트 조회 | 상대적으로 단순한 폼/리스트                                                                                                                   |
| 3 (여유 있으면)        | 시니어 내 정보, 보호자 내 정보, 시니어 이전 대화 기록 조회                                                            | 원래 "가장 먼저 스코프 컷 대상"이었으나 실제로는 이미 실장되어 있음(Figma 기준 디자인+연동 진행) — 스코프 컷 순위 재조정 필요                 |

## 최종 크런치 일정 (8/15~8/20, Week 1/2 표 대체)

전제: 8/15(토)~8/17(월) AI서버/백엔드 담당 1인 단독 작업, 8/18(화)~8/20(목) 전원 작업, 8/21 이후 미확정이라 이 안에 골든패스+필수 범위를 전부 끝내야 한다. 로그인 API·AI서버 프로토콜 불일치는 8/14에 이미 해소됨, 감정분석 실모델·TTS AI서버 부분은 8/15에 이미 해소/우회 정책화됨 — 아래는 그 위에서 남은 작업만 반영한다.

### 8/15 (토) — 나 혼자 (AI서버+백엔드 겸업)

- ~~SGDS-K/GAD-7/LSNS-6 척도 채점 실구현~~ **완료 (계획보다 앞섬)**: `apps/ai-server/app/services/llm_prompts.py`에 3개 척도 문항 은행(28문항, 위험 응답 방향 포함)을 고정 텍스트로 넣고 SYSTEM_PROMPT 채점 규칙에 반영, `llm.py`는 `SCALE_ANALYSIS_MODE=model`일 때 이 실채점 결과를 사용하도록 분기(`test`/`empty`는 기존 stub 유지). AI 페르소나도 '다슬'로 명시함.
- 남은 것 (8/16 아침으로 이월): **실제 `OPENAI_API_KEY`로 채점 품질 검증** — 지금까지는 unit test(mock)만 통과했고 실제 LLM 호출로 확인한 적 없음. `.env`에 `SCALE_ANALYSIS_MODE=model` 설정 후 `scripts/test_llm.py`로 여러 발화 패턴 돌려보고, 필요하면 문항 은행/프롬프트 문구 조정.
- 목표(재확정): 3개 척도 전부 코드 완료 + 최소 1회 실제 API 호출로 채점 결과 확인

### 8/16 (일) — 나 혼자

- 아침: 8/15에서 이월된 **실제 API 채점 검증** 먼저 마무리
- **문항 커버리지 컨텍스트 전달** (8/15 신규 발견 리스크) — NestJS `QuestionAnswerBatch` 계약(`audio-analysis.contract.ts`)과 FastAPI `/analysis/audio/batch` 엔드포인트(`main.py`) 양쪽에 "오늘 이미 채점된 문항" 정보를 넘기는 필드 추가, AI서버가 이걸로 실제 `SessionState.pending_scale_items`를 채우게 연결. 이게 없으면 다슬이가 같은 문항을 계속 반복해서 물어볼 수 있음
- **TTS 배관 복구** — AI서버(`schemas.py`/`main.py`)는 이미 `ttsAudioBase64`/`ttsMimeType`을 응답에 포함하도록 완료돼 있음. 남은 건: 백엔드 `analysis` 계약/검증기(`audio-analysis.contract.ts`)에 해당 필드 반영 → `tts:audio` WS 이벤트 실제 emit → 프론트 TTS 오디오 재생 컴포넌트 신규 추가(현재 전무) → `docs/ws-protocol.md` §6.2/§8도 이 방식(배치 base64)으로 갱신(현재 "MVP 이후"에 잘못 남아있음)
- 목표: 3개 척도 채점 실검증 완료 + 문항 반복 방지 연결 + TTS 오디오가 실제로 프론트까지 도달

### 8/17 (월) — 나 혼자

- 정서지수 즉시 재계산 트리거(대화 종료 / 같은 날 재대화 / 10분 유휴 시점) 연결 — `report-generation-coordinator.service.ts`에는 여전히 09:00 cron 하나뿐이므로 새로 추가해야 함
- 시니어 본인 발화 STT 결과 실시간 반영 — 현재 placeholder 말풍선만 있고 실제 텍스트로 안 바뀜, 새 WS 이벤트+shared-types 타입+프론트 말풍선 갱신 필요
- 저녁: 로그인→WS 인증→마이크 답변→STT 반영→척도 채점→질문 생성→TTS 재생까지 브라우저 셀프 E2E, 발견 버그 즉시 수정 — 이때 같은 문항을 반복해서 묻지 않는지(8/16에 연결한 커버리지 컨텍스트)도 같이 확인
- 목표: **"음성 대화 챗봇 완성" 선언 가능한 상태**

### 8/18 (화) — 전원

| 담당 | 작업 |
| --- | --- |
| BE | 웹 푸시 백엔드는 이미 ~90% 완료(`push-subscriptions.service.ts`, `web-push-delivery.service.ts`, VAPID 설정, `notifications.service.ts` 실발송 호출 전부 확인됨) — 남은 건 프론트 연동 지원과 VAPID 키 실제 env 값 세팅 |
| FE | 알림 권한 요청 UI + SW `pushManager.subscribe` 구독 등록 플로우(현재 SW는 등록만 돼 있고 구독 로직 없음) → 백엔드 구독 저장 API 연동 |
| AI서버 | **감정분석 실모델 연결 착수** — `EMOTION_MODE=model`로 전환, 텍스트/음성 감정분류 체크포인트 확보(공개 사전학습 모델 사용 가능, `MODEL_POLICY.md`의 라벨 정의·융합 방식 기준). 컷 대상 아님 — 8/20 이전 완료 필수, 하루 안에 안 끝나면 8/19로 이어서 진행 |
| 나 | 어제 완성한 파이프라인을 팀과 함께 재검증, 발견된 버그 페어 수정 |

### 8/19 (수) — 전원

| 담당 | 작업 |
| --- | --- |
| BE | 위험 임계치 초과 시 실제 웹 푸시 발송 최종 확인, `notifications` 알림함 조회 엔드포인트 프론트 연동 지원 |
| FE | mock 데이터로 남아있는 화면 4종을 실 API로 교체: 보호자 대시보드 홈(`GET /guardian/dashboard`), 보호자 알림함, 보호자 일간/주간 리포트(캘린더+근거 메시지 포함), 시니어 이전 대화 기록 조회 — 백엔드는 전부 완료돼 있어 연동만 하면 됨 (회원가입·연결 화면 2개는 이미 구현+연동 완료 확인됨 — 작업 목록에 없음) |
| AI서버 | 8/18에 못 끝냈다면 감정분석 실모델 연결 마무리 — **오늘 안에 반드시 완료**(내일이 최종 체크포인트) |

### 8/20 (목) — 전원, 사실상 최종 체크포인트

- 로그인→WS 인증→마이크 답변→STT 반영→척도 채점→질문 생성→TTS 재생→리포트 갱신→위험 알림 발생 시 푸시 수신까지 전체 흐름을 브라우저에서 실제로 완주(이 시점엔 감정분석도 실모델 기준으로 동작해야 함)
- 잔여 버그 정리 + 데모 리허설
- **8/21 이후 일정이 불확실하므로, 오늘 안에 "데모 가능한 최소 상태"를 반드시 확정한다**

Redis 기반 완전 재연결 복구(서버 재시작/다중 인스턴스 상태 복원)와 위험 키워드 실시간 감지(자해/자살 신호 등 실시간 알림 트리거), 관리자 화면은 이번 스프린트 범위에서 완전히 제외 확정(2026-08-16). TTS는 청크 단위 실시간 스트리밍이 아니라 배치 합성 후 base64 일괄 전송 방식으로 가되, 기능 자체(질문을 음성으로 들을 수 있음)는 8/17까지 완성한다.

## 스코프 컷 없음 — 8/20까지 전부 완료 전제

**이번 크런치 일정은 스코프 컷을 계획하지 않는다.** 화면 12개 기능 전부, 음성 대화 챗봇(척도 채점 3개·TTS·정서지수 즉시 재계산·STT 실시간 반영 포함), 웹 푸시까지 8/20 안에 전부 끝낸다는 전제로 위 일별 작업을 배정했다. 특정 항목이 늦어질 조짐이 보이면 그 항목만 조용히 빼는 대신, 그날 스탠드업에서 바로 공유해서 남은 날짜에 인력을 재배치한다.

## 작업 방식

- 매일 15분 스탠드업 (비동기도 가능)
- 8/20(목)이 사실상의 스프린트 리뷰이자 최종 체크포인트 — 8/21 이후 일정이 불확실하므로 여기서 반드시 데모 가능한 최소 상태를 확정
- AI서버 담당이 가장 리스크 큰 자리(3개 벤더 통합 + 프롬프트 엔지니어링 + 이번에 새로 드러난 프로토콜 재작업까지 겹침) — 막히면 BE 담당이 페어링
- **BE↔AI 프로토콜은 다시 합의하는 즉시 `docs/ws-protocol.md`(또는 별도 REST 계약 문서)에 문서화** — 이번처럼 말로만 합의하고 각자 다르게 구현하는 상황 재발 방지

## Claude → Codex 작업 인수인계 규칙

프론트 화면은 Claude가 1차 구현 후 Codex로 디자인을 보완하는 흐름을 씁니다.

- **스타일 위치**: 시각적 값(색상/spacing/폰트 크기)은 tsx에 인라인으로 넣지 않고 해당 컴포넌트의 `.module.css`에 둔다 (기존 코드베이스 컨벤션 유지)
- **마크업**: Figma 스펙 반영에 필요한 만큼만 — 새 wrapper 컴포넌트를 미리 만들지 않는다
- **클래스명**: 의미 기반 네이밍(`.scoreBadge`, `.avatarWrapper` 등)으로, Codex가 요소를 정확히 찾아 수정할 수 있게 한다
- **인수인계 노트** (PR 설명에 포함, 별도 문서 만들지 않음):
  - 이번에 건드린 파일 목록 (tsx + module.css)
  - 참고한 Figma 레퍼런스 경로(`docs/screens/.../screen-*.png`)와 반영 못 한 `states/` 케이스
  - **스코프 가드**: "이 파일들의 로직(WS 핸들러, API 호출, 상태관리)은 건드리지 말고 className/마크업/CSS 값만 수정" 명시
- Codex 작업 완료 후 `git diff`로 `.module.css`/마크업 외 로직 파일이 바뀌었는지 확인
- BE/AI서버의 핵심 로직(채점 알고리즘, 인증, WS 이벤트 처리)은 Claude 구현 후 Codex에게 검증(버그/보안) 요청하는 용도로 사용 — 디자인 보완과는 별개 트랙

## 날짜별 실행 프롬프트

팀원 각자 자기 Claude Code 세션에 그대로 입력하면 되는 프롬프트입니다. 이 저장소를 열어둔 상태에서 입력하면 `docs/sprint-plan.md`, `docs/ws-protocol.md`, `docs/screens`, `packages/shared-types`를 참고해서 작업합니다.

### Day 0 (완료, 기록용)

- **전원**: "오늘은 Day 0 킥오프야. `docs/ws-protocol.md`를 기준으로 FE↔NestJS, NestJS↔FastAPI 계약(이벤트 필드, DTO, 타임아웃 등)을 확정하고, `packages/shared-types`를 이 문서 기준으로 재정비해줘. 옛 타입(`ConversationTurn`, `VoiceCapturedEvent` 등)을 참조하는 프론트 파일도 찾아서 마이그레이션 대상으로 목록화해줘."
- 실제 결과: FE↔NestJS 계약은 잘 맞았으나, **NestJS↔FastAPI 계약은 합의대로 구현되지 않았음**(AI서버가 REST 배치 대신 자체 WS 세션 프로토콜을 구현) — 2026-08-13 재조사에서 발견, 위 "신규 리스크" 참고.

### Day 1 (완료 vs 미완료 섞임, 기록용)

- **FE/PM/UX**: "오늘은 PWA 셸(manifest+SW) + 로그인 화면 디자인 적용 및 연동을 할 거야. ..." — 실제로는 로그인 화면 디자인은 Codex 트랙으로 넘어갔고, 연동은 백엔드 로그인 API 부재로 막혔음. PWA 셸도 착수되지 않음 — 둘 다 Day 2로 이연.
- **BE/DB (8/12)**: "현재 구현된 WS 이벤트, `packages/shared-types`, `docs/ws-protocol.md`를 비교해 이름과 payload 불일치를 정리해줘. ..." — 이 부분은 완료됐고 드리프트 해소됨(2026-08-13 재조사 확인).
- **AI서버**: "오늘은 Day 0 계약 확정에 참여하고, faster-whisper STT 엔드포인트를 구현할 거야. ..." — STT는 실제로 완성됐으나, 이때 BE↔AI REST 계약 대신 자체 WS 프로토콜 방향으로 진행된 것으로 추정됨.

### Day 2 (오늘, 8/13)

- **BE/DB**: "오늘은 두 가지가 최우선이야. 1) `apps/backend/src/auth`에 실제 로그인(sign-in) 엔드포인트(`POST /auth/login`)를 구현해줘 — 아이디/비밀번호 검증 후 JWT 발급까지. 기존 `check-login-id`/`signup`과 같은 컨벤션으로. 2) AI서버 팀원과 함께 `apps/ai-server`의 실제 프로토콜(`/ws/counsel/{session_id}`)과 `apps/backend/src/analysis/ai.client.ts`가 기대하는 `POST /analysis/audio/batch` REST 배치 계약이 서로 다르다는 걸 확인하고, 어느 쪽으로 통일할지 정리해줘."
- **FE/PM/UX**: "오늘은 PWA 셸(manifest + service worker)을 구현할 거야. 백엔드 의존 없는 작업이니 로그인 API를 기다리지 않고 바로 진행해줘. 설치 가능한 PWA 최소 조건(manifest.json, 기본 SW 등록)까지."
- **AI서버**: "오늘은 백엔드 팀원과 함께 AI서버 프로토콜(`/ws/counsel/{session_id}`)과 백엔드가 기대하는 REST 배치 계약(`POST /analysis/audio/batch`)의 불일치를 확인하고 통일 방향을 정할 거야. 그 후 SGDS-K/GAD-7/LSNS-6 중 GAD-7 채점 로직부터 설계 착수해줘(지금 척도채점 코드가 전혀 없는 상태야)."

### Day 3 (8/14)

- **FE/PM/UX**: "로그인 API가 준비됐으면 `mockResolveRole()`을 제거하고 실제 로그인 연동으로 교체해줘. 성공하면 그 즉시 시니어 대화 화면에서 실제 브라우저로 로그인→WS 인증→대화 1턴까지 확인해줘(WS 클라이언트 코드는 이미 구현돼 있어)."
- **BE/DB**: "로그인 엔드포인트가 프론트와 잘 붙는지 확인해주고, 이제 웹 푸시 인프라를 시작할 거야. `PushSubscription` 저장용 엔티티와 구독 등록 REST 엔드포인트, VAPID 키 발급을 구현해줘."
- **AI서버**: "어제 합의한 프로토콜 방향으로 백엔드와의 연동을 재작업해줘. 그리고 감성분석 모델 로딩부(현재 TODO 스텁)를 실제 모델 연결로 바꿔줘."

> **2026-08-15 이후 일정 재구성**: 아래부터는 8/13 시점 Day 4~10(8/17~8/25) 계획을 대체한다. 인원 가용성이 바뀌어 8/15(토)~8/17(월)은 담당자 1인 단독 작업, 8/18(화)~8/20(목)은 전원 작업이며, **8/21 이후는 계획하지 않는다**(위 "최종 크런치 일정" 참고).

### 8/15 (토) — 나 혼자 (완료, 기록용)

"오늘은 SGDS-K/GAD-7/LSNS-6 척도 채점을 실구현할 거야. `apps/ai-server/app/services/llm.py`의 `_stub_answer_analyses()`가 항상 빈 배열을 반환하는 스텁 상태니까, 이걸 제거하고 실제 채점 프롬프트(문항 유도 → 응답 → 위험여부 0/1 판정)를 넣어줘. 검증 로직(`_validate_scale_analysis_item`)은 이미 있으니 그 계약 형태에 맞춰줘. GAD-7부터 먼저 완성해서 최소 1개 척도는 오늘 안에 실제 결과가 나오게 해줘."

- 실제 결과: GAD-7만이 아니라 **3개 척도 전부 코드 완료**됨 — 문항별로 나눠 짤 필요 없이 문항 은행 하나(`llm_prompts.py`의 `SCALE_ITEM_BANK`)로 한 번에 처리. 다만 실제 `OPENAI_API_KEY` 호출로 검증은 안 함(8/16으로 이월), 문항 커버리지 컨텍스트 전달 안 됨/`ws-protocol.md` TTS 섹션 stale 두 가지 신규 리스크 발견 — 위 "2026-08-15 저녁 업데이트" 참고.

### 8/16 (일) — 나 혼자

"먼저 어제 짠 척도 채점을 실제로 검증할 거야 — `.env`에 실제 `OPENAI_API_KEY`를 넣고 `SCALE_ANALYSIS_MODE=model`로 바꾼 다음 `scripts/test_llm.py`로 여러 발화를 넣어서 채점이 그럴듯하게 나오는지 확인해줘. 문제 있으면 `llm_prompts.py`의 문항 은행/프롬프트 문구를 조정해줘.

그다음 문항 커버리지 컨텍스트를 연결할 거야 — 지금 NestJS `QuestionAnswerBatch` 계약(`audio-analysis.contract.ts`)과 FastAPI `/analysis/audio/batch` 엔드포인트(`main.py`) 둘 다 '오늘 이미 채점된 문항' 정보를 넘기는 필드가 없어서, AI서버가 같은 문항을 계속 반복해서 물어볼 수 있어. 이 필드를 계약 양쪽에 추가하고 AI서버의 `SessionState.pending_scale_items`를 실제로 채워줘.

마지막으로 TTS 배관을 복구할 거야 — AI서버(`schemas.py`/`main.py`)는 이미 `ttsAudioBase64`/`ttsMimeType`을 응답에 담아 보내고 있으니, 백엔드 `analysis` 계약(`audio-analysis.contract.ts`)과 검증기에 이 필드를 반영하고, `tts:audio` WS 이벤트를 실제로 emit하도록 만들어줘. 프론트에 TTS 오디오 재생 컴포넌트를 새로 추가해줘(현재 전혀 없어) — base64를 디코드해서 재생하면 돼. 끝나면 `docs/ws-protocol.md`도 이 방식(배치 base64)으로 갱신해줘 — 지금 §8 'MVP 이후'에 TTS binary 전송이라고 잘못 남아있어."

### 8/17 (월) — 나 혼자

"오늘은 두 가지를 끝낼 거야. 1) 정서지수 즉시 재계산 트리거 — 지금 `report-generation-coordinator.service.ts`는 매일 09:00 cron 하나뿐이니, 대화 종료 시점 / 같은 날 재대화 시점 / 10분 유휴 시점에 즉시 재계산하는 경로를 추가해줘. 2) 시니어 본인 발화 STT 실시간 반영 — 지금은 placeholder 말풍선만 있고 실제 텍스트로 안 바뀌니까, 새 WS 이벤트를 `packages/shared-types`에 정의하고 백엔드에서 emit, 프론트에서 말풍선을 갱신하도록 연결해줘. 끝나면 로그인→WS 인증→마이크 답변→STT 반영→척도 채점→질문 생성→TTS 재생까지 브라우저로 직접 셀프 E2E 테스트하고, 이때 같은 척도 문항을 반복해서 묻지 않는지(8/16에 연결한 커버리지 컨텍스트)도 같이 확인해줘. 발견되는 버그는 그 자리에서 고쳐줘. 오늘 목표는 '음성 대화 챗봇 완성'이라고 말할 수 있는 상태야."

### 8/18 (화) — 전원

- **BE/DB**: "웹 푸시 백엔드는 이미 거의 다 돼 있어(`push-subscriptions.service.ts`, `web-push-delivery.service.ts`, VAPID 설정, `notifications.service.ts`의 실발송 호출까지). VAPID 키를 실제 env 값으로 세팅하고, 프론트 구독 연동을 도와줘."
- **FE/PM/UX**: "알림 권한 요청 UI와 SW `pushManager.subscribe` 구독 등록 플로우를 붙여줘(현재 SW는 등록만 돼 있고 구독 로직이 없어). 백엔드 구독 저장 API에 실제로 등록되는지 확인해줘."
- **AI서버**: "지금 `EMOTION_MODE=test`로 감정값을 고정해서 우회하고 있는데, 이제 실제 모델로 전환할 거야. `apps/ai-server/models/MODEL_POLICY.md`의 라벨 정의·융합 방식 기준으로 텍스트/음성 감정분류 체크포인트를 확보하고 `EMOTION_MODE=model`로 전환해줘. 컷 대상 아니고 8/20 전엔 반드시 끝나야 해 — 오늘 안에 안 끝나면 내일 이어서 해줘."
- **나**: "어제 완성한 음성 대화 파이프라인을 팀과 함께 다시 검증하고, 발견되는 버그를 같이 고칠 거야."

### 8/19 (수) — 전원

- **BE/DB**: "위험 임계치 초과 시 실제 웹 푸시 발송이 되는지 최종 확인하고, `notifications` 알림함 조회 엔드포인트 프론트 연동을 도와줘."
- **FE/PM/UX**: "지금 mock 데이터로 남아있는 화면 4개를 실 API로 교체해줘: 보호자 대시보드 홈(`GET /guardian/dashboard`), 보호자 알림함, 보호자 일간/주간 리포트(캘린더+근거 메시지 포함), 시니어 이전 대화 기록 조회. 백엔드는 전부 완료돼 있으니 연동만 하면 돼. (회원가입·연결 화면 2개는 이미 구현+연동 완료 확인됐으니 손댈 필요 없어.)"
- **AI서버**: "어제 감정분석 실모델 전환을 못 끝냈으면 오늘 반드시 마무리해줘 — 내일이 최종 체크포인트라 오늘이 마지노선이야."

### 8/20 (목) — 전원, 최종 체크포인트

- **전원**: "오늘이 사실상 마지막 체크포인트야(내일부터 일정이 불확실해). 로그인→WS 인증→마이크 답변→STT 반영→척도 채점→질문 생성→TTS 재생→리포트 갱신→위험 알림 발생 시 푸시 수신까지 전체 흐름을 브라우저에서 실제로 끝까지 완주해줘. 안 되는 지점 있으면 바로 공유하고, 오늘 안에 데모 가능한 최소 상태를 반드시 확정해줘."
