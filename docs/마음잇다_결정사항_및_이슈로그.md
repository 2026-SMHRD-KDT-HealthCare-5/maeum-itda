# 마음잇다 - 결정사항 및 미결 이슈 로그

> 기획서 / 요구사항정의서 / 화면설계서, 그리고 화면별 상세 목업인 `docs/page-pdf/`(커밋됨)와 함께 참고할 것. `docs/page-html/`도 같은 목업의 인터랙티브 버전이지만 용량 때문에 `.gitignore` 처리되어 있어 로컬에만 있을 수 있음 — 있으면 참고하되 항상 존재한다고 가정하지 말 것.
> 아래 내용은 문서 검토 과정에서 별도로 논의·확정된 사항이다. 2026-08-28자로 3개 원본 문서(및 page-html/page-pdf 목업)가 갱신되면서 이 로그의 결정사항 중 일부는 이미 문서에 반영되었고, 일부는 오히려 **번복**되었다 — 아래 표의 "최종 문서 반영 상태" 열을 항상 확인할 것. 이 열이 "미반영"인 항목만 여전히 문서보다 이 로그를 우선한다.

---

## 1. 확정된 MVP 결정사항

| 항목 | 확정 내용 | 최종 문서(2026-08-28) 반영 상태 |
|---|---|---|
| 보호자-시니어 연결 기능 | ~~MVP 제외~~ | **번복됨.** 요구사항정의서에 UC-00-1(보호자 → 시니어 연결 요청 및 승인)이 신설되었고, 화면설계서에 화면ID `SENIOR_LINK_01`/`GUARDIAN_LINK_01`가 배정되었으며, `docs/page-html/시니어 및 보호자 연결 화면.html`·`docs/page-pdf/시니어 및 보호자 연결 화면.pdf`에 아이디/휴대폰번호 검색 → 연결 요청 → 수락/거절 → 연결 해제까지 전체 플로우 목업이 존재한다. **이제 이 기능은 MVP 포함으로 확정.** 이전 행의 "관리자가 매핑을 직접 설정" 가정은 폐기 |
| 정서 상태 라벨 | 좋음 / 보통 / 나쁨 3단계 | **반영됨.** `docs/page-pdf/보호자 대시보드.pdf`, `보호자 일간 리포트 조회.pdf` 목업에 좋음/보통/나쁨 배지·색상(초록/노랑/빨강)이 실제로 표시됨. 점수 구간 경계값은 화면상 "낮게 30 / 보통 50 / 높게 70" 임계치 프리셋으로 일부 구체화됨(`보호자 알림 설정.pdf`) |
| 고위험 알림 트리거 시점 | MVP: 일간 리포트 생성 완료 시점 판단. 고도화: 실시간 감지 | **문서와 실제 결정이 어긋남 — 문서를 따르지 말 것.** 요구사항정의서 FR-03-03에 "②대화 중 자살·자해 등 고위험 키워드가 직접 검출되는 경우" 즉시 푸시 알림 조건이 이미 추가되어 있고, `보호자 알림 설정.pdf` 목업에도 끌 수 없는 "위험 신호 즉시 알림"(항상 ON) 토글이 그려져 있다. 그러나 **팀 결정: 실시간 고위험 키워드 감지는 여전히 MVP 범위 밖이다** — 문서/목업의 해당 문구·컴포넌트는 고도화 단계 대비용으로 앞서 작성된 것일 뿐, MVP 구현 대상이 아니다. `apps/backend`/`apps/ai-server`에서 이 실시간 감지 로직을 지금 구현하지 말 것 |
| SGDS-K 데이터 부족 시 처리 | 고도화 단계에서 결정 | **미반영, 그대로 유효.** 최종 요구사항정의서에도 관련 API 응답 스키마(`emotionScore` null 처리 등) 확정 내용 없음 |
| 백엔드 기술스택 | TypeScript, Node.js, Nest.js (기획서의 "Express" 표기는 오기) | **반영됨(오기 자체가 사라짐).** 최종 기획서 3쪽·8쪽 "사용 기술"에 "TypeScript, Node.js, Nest.js"로 명시, "Express" 언급 전무 |
| DB 기술스택 | MySQL (요구사항정의서 NFR-SE-003의 "PostgreSQL" 표기는 오기) | **반영됨(오기 자체가 사라짐).** 최종 요구사항정의서 NFR-SE-003·FR-04-01, 기획서 모두 MySQL로 통일. "PostgreSQL" 문자열 전무 |
| 관리자 화면(데이터 품질 검토) | 기획만 하고 MVP 구현 범위에서 제외 | **미반영이지만 그대로 유효.** 화면설계서 메뉴구성 트리에는 남아있으나(3쪽), 8개 화면ID 목업(5~12쪽) 중 관리자 화면은 없음 — 여전히 목업조차 없는 상태이므로 MVP 제외 결정 유지 |
| UC-10 (정서지수 하락 알림 수신) 처리 방식 | 별도 화면/UI 없이 **UC-11(알림함)에 흡수**. 알림함 목록에 다른 유형과 동일하게 표시됨 | 문서에 명시적 통합 선언은 없으나(§2-1의 일간/주간 트리거 통합 이슈와는 별개), 팀 결정으로 확정. `apps/frontend`의 `mark-notification-read` 슬라이스가 UC-10/UC-11을 함께 처리함 |
| 웹 푸시 알림(실제 push 발송) 구현 시점 | **MVP 범위 아님.** 이 프로젝트는 먼저 웹앱으로 구현하고, 이후 PWA를 적용하는 시점에 Service Worker/Push API 기반 푸시를 별도로 추가할 계획. MVP에서는 알림함(REST 조회)만으로 알림을 전달함 | 문서에는 언급 없음 — 팀의 로드맵 결정. `apps/backend`/`apps/frontend`에 지금 Service Worker나 웹 푸시 관련 코드를 미리 만들지 말 것 |
| UC-14 (대화 빈도 기반 캐릭터 환경 꾸미기) | **구현하지 않음** | **반영됨(문서에서 완전히 삭제).** 요구사항정의서 재수정판에서 UC-14 자체가 사라짐("UC-14", "캐릭터", "꾸미기" 문자열이 문서 전체에 0건) — 더 이상 "팀이 존재하는 UC를 제외"하는 상황이 아니라 문서에도 없는 UC. `FR-01-08`은 같은 ID를 재사용해 "음성 분석 실패 처리"라는 별개 내용으로 바뀌었으니 혼동하지 말 것 |

---

## 2. 아직 안 끝난 것 - 다음 논의에서 이어가야 할 이슈

1. **일간 vs 주간 알림 트리거 통합 여부**
   최종 요구사항정의서에도 여전히 언급 없음. UC-08(일간/주간 리포트 조회)은 조회 전용 UC로만 존재하고, UC-10(정서지수 하락 알림)은 일간 단위 트리거만 서술 — 주간 트리거와의 통합/분리 여부 미결정 그대로.

2. **알림 `target` 스키마 확장**
   **부분 해결.** 최종 요구사항정의서 UC-11 기본흐름③에 알림 목록 반환 필드가 "유형, 제목, 내용, 대상(리포트 등)" 4-튜플로 구체화되었고, FR-03-04에 알림 유형 3종(연결 요청 / 정서지수 하락 / 리포트 도착)이 명시됨. 다만 유형별 `target` 필드의 실제 타입 정의(예: `{type:'dailyReport', reportId}` 같은 스키마)는 여전히 없음 — `packages/shared-types` 설계 시 확정 필요.

3. **근거 문장(evidenceSentences) 소스 범위**
   **문서상 해결, 그런데 화면 목업과 다시 어긋남.** 요구사항정의서 UC-06-1/FR-02-02/UC-09는 근거 문장 추출을 SGDS-K·GAD-7·LSNS-6 척도 태그가 붙은 발화로 명확히 한정한다(일상 대화는 채점 생략). 그런데 `docs/page-pdf/보호자 일간 리포트 조회.pdf`의 "대화 근거 행" 섹션은 척도 여부와 무관하게 모든 Q&A 스니펫에 감정 태그(긍정/보통/부정/위험)를 붙여 노출한다. 즉 API/UC 스펙은 척도-한정이지만 화면 목업은 전체 대화 노출로 보인다 — 구현 전 재확인 필요.

4. **문서 내 오탈자** — **해결됨.** DB는 MySQL로 통일(위 표 참고). 3개 원본 문서 표지 날짜도 모두 2026.08.28로 통일되어 역전 문제 해소. (참고: 화면설계서 내부 변경이력 표의 최신 리비전(v0.3)이 2026.08.03으로 표지 날짜보다 앞서 있는 사소한 불일치가 남아있으나, 별도 조치 불필요한 수준)

5. **`voice_score` 이름 충돌 (요구사항정의서 재수정판에서 신규 발견)**
   UC-07은 각 응답(turn)마다 `voice_score`를 저장한다고 명시하는데, UC-06-2의 정서지수 공식(`TextScore × 0.7 + VoiceScore × 0.3`)의 `VoiceScore`는 하루 단위 집계값이다. 이름이 같아 보이지만 turn 단위 값과 일간 집계값은 다른 것으로 보임 — 문서가 둘의 관계(집계 방식 등)를 설명하지 않아 확정 필요. `entities/conversation`의 `ConversationTurn.voiceScore`는 일단 turn 단위 값으로 가정해뒀음(주석 참고).

---

## 3. 참고 사항 (별도 조치 불필요)

- 시스템 아키텍처 다이어그램(클라이언트 - 백엔드 API 서버 - AI 서버 - MySQL DB - 알림 서버 4계층 구조)은 위 3개 문서 내용만을 시각화한 것으로, 로드밸런서·캐시·CDN 등 인프라 요소는 문서에 명시되지 않아 제외됨. 실제 배포 인프라가 정해지면 별도로 그릴 것.

---

## 4. 향후 확장 대비 아키텍처 원칙

> 팀 전체가 공식적으로 확정한 요구사항은 아니고, 외부 조언을 받아 개발 방향에 반영하기로 한 엔지니어링 원칙. MVP는 웹으로만 구현하되, 나중에 Flutter 앱을 추가할 때 백엔드/AI서버를 다시 짜지 않아도 되도록 아래를 지킨다.

- 백엔드(NestJS) REST API는 OpenAPI(Swagger) 스펙으로 관리 — 추후 Flutter(Dart)용 클라이언트를 스펙 기반으로 생성 가능하게 함
- 실시간 안부 대화 WebSocket 이벤트명·payload 구조는 `packages/shared-types`에 타입으로 고정 — 프론트엔드 코드 안에만 암묵적으로 존재하지 않도록 함
- 인증은 브라우저 쿠키/세션이 아닌 JWT 등 토큰 기반으로 구현 — 모바일 클라이언트도 동일한 인증 흐름 재사용 가능하게 함
- 정서지수 계산, 임계치 판단, 권한 체크 등 비즈니스 로직은 프론트엔드(React)가 아닌 백엔드/AI서버에 위치 — 새 클라이언트가 로직을 재구현할 필요 없게 함
